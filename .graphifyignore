# Not our product to analyse. graphify already skips node_modules, dist, build,
# target, out, venv, coverage and __snapshots__ by default, and merges each
# directory's .gitignore — so frontend dependencies and generated trees are
# already out. These are the cases those defaults do not cover.

# Agent tooling state — session/swarm bookkeeping, not source we own.
.claude/
.claude-flow/
.swarm/
.agentic-qe/

# Vendored duplicates: this tree is graphed from its own checkout. Including a
# vendored copy as well mints two nodes for every class and invents edges
# between them.
extraction/
