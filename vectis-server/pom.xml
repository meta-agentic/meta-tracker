<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.vectis</groupId>
    <artifactId>vectis-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>vectis-server</artifactId>
  <name>Vectis :: Server (composition root)</name>
  <description>
    The runnable Quarkus application. Depends on vectis-core but NOT on any
    commercial plugin, so a default build is the "commercial JAR omitted"
    deployment: it must boot and serve with community capability only.
  </description>

  <dependencies>
    <dependency>
      <groupId>io.vectis</groupId>
      <artifactId>vectis-core</artifactId>
    </dependency>

    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>

    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
              <goal>generate-code</goal>
              <goal>generate-code-tests</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Live end-to-end demonstration of AC #1/#3 discovery: build with
      -Pwith-enterprise-audit to drop the example commercial plugin onto the
      server's runtime classpath and enable its license flag. The default
      build (this profile OFF) is the "plugin omitted" deployment.
    -->
    <profile>
      <id>with-enterprise-audit</id>
      <dependencies>
        <dependency>
          <groupId>io.vectis</groupId>
          <artifactId>vectis-plugin-audit-enterprise</artifactId>
        </dependency>
      </dependencies>
    </profile>
  </profiles>
</project>
