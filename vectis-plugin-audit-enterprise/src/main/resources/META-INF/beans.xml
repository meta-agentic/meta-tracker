<?xml version="1.0" encoding="UTF-8"?>
<!--
  Marks this JAR as a CDI bean archive so ArC indexes it when it is present on
  the deployment classpath. This is what makes the plugin discoverable purely
  by being packaged into the image — the core needs no registration entry.
-->
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       bean-discovery-mode="annotated"
       version="4.0">
</beans>
